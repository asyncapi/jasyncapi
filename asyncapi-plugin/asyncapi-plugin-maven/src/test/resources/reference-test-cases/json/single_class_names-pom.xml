<project>
    <build>
        <plugins>
            <plugin>
                <groupId>com.asyncapi</groupId>
                <artifactId>asyncapi-maven-plugin</artifactId>
                <configuration>
                    <classNames>com.asyncapi.plugin.maven.asyncapi.streetlights.StreetlightsAsyncAPI</classNames>
                    <schemaFileFormat>json</schemaFileFormat>
                    <schemaFilePath>target/generated-test-sources/Complete</schemaFilePath>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
